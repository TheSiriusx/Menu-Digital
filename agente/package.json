{
  "name": "agente-panaderias",
  "private": true,
  "type": "module",
  "description": "Asistente de WhatsApp para panaderías (Evolution API + OpenRouter + menú digital en Supabase)",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning src/main.ts",
    "test": "node --disable-warning=ExperimentalWarning --test test/*.test.ts",
    "check": "tsc -p ."
  }
}
